Cheat Sheet for git Commands
-1
-2
-3
-4
-5
-6 Let's configure the editor to VS code (already installed in labs), run the following in the command line: git config --global core.editor "code --wait" 
